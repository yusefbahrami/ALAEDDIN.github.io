import React from "react";
import ReactAudioPlayer from "react-audio-player";
// import file from 

const MusicPlayer = (props) => {
    const basePath = "../../assets/Sound/02 Laanat.mp3";

  console.log(props.m.trim());
//   console.log(file);
//   return <ReactAudioPlayer key={Math.random()} src={`${file}`} controls />;
};
export default MusicPlayer;
